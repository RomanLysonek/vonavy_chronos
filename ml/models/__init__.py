"""Active model implementations for the two-contender challenge.

``neural_net`` is the frozen project-specific incumbent and ``chronos2_model``
is the zero-shot foundation-model adapter. Modules are imported explicitly by
``pipeline.py`` to keep optional Chronos dependencies lazy.
"""
